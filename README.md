# Web-Scraping-Demo
This script scrapes `books.toscrape.com` using `requests` and `BeautifulSoup` for book data (titles, prices, ratings). It then cleans data with `pandas` (float prices, numerical ratings) and visualizes distributions (prices, ratings) using `matplotlib` and `seaborn`. A practical example of web scraping, cleaning, and analysis in Python.
